spring:
  application:
    name: NexWallet

  data:
    mongodb:
      uri: mongodb://localhost:27017/cryptotracker
    redis:
      host: localhost
      port: 6379

nexwallet:
  client:
    coingecko: https://api.coingecko.com/api/v3